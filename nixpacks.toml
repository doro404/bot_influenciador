[phases.setup]
nixPkgs = ["ffmpeg", "python3"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["python setup_railway.py"]

[start]
cmd = "python bot.py" 